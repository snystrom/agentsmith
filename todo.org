#+title: Todo
* DONE evil mode keybinds
-
* DONE do not ask for branch name on worktree creation. Also, when you create the worktree currently you are using the reponame as the worktree name. don't do that: instead use the workspace name as the worktree name!
* TODO update enter behavior for the

* DONE Pass an agent method configuration object rather than the funky hardcode claude-code-ide stuff in `agentsmith-agent.el` now
ex want something more like:

(agentsmith-config-alist
'(
 start-method '#claude-code-ide-start
 stop-method '#claude-code-ide-stop
))

...etc
* TODO project agent management
** agentsmith-open-workspace-agent
** agentsmith-open-worktree-agent

* TODO popup buffer to select an agent in the workspace
** ex will list each worktree name & allow user to type / scroll and press enter to open the agent
